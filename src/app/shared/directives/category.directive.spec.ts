import { CategoryDirective } from './category.directive';

describe('CategoryDirective', () => {
  it('should create an instance', () => {
    const directive = new CategoryDirective();
    expect(directive).toBeTruthy();
  });
});
